<div class="page-header">
  <h1>New Search Synonym</h1>
</div>

<%= simple_form_for @search_reference, url: synonyms_commodity_search_references_path(commodity), html: { class: 'search-reference-form' } do |f| %>
  <div class='row'>
    <div class='col-sm-6'>
      <%= f.input :title, label: 'Synonym',  input_html: { class: 'form-control' } %>
    </div>
  </div>

  <div class='form-actions'>
    <%= f.button :submit, class: 'btn btn-success', data: { 'disable-with' => 'Working' } %>
    <%= link_to "Back", :back, class: 'btn' %>
  </div>
<% end %>
